diff --git a/kernel/Makefile b/kernel/Makefile
index d3a3f45..bbb53a0 100644
--- a/kernel/Makefile
+++ b/kernel/Makefile
@@ -116,6 +116,11 @@ CFLAGS_ksu.o += $(call cc-option, -std=gnu17)
 CFLAGS_ksu.o += $(call cc-option, -std=gnu23)
 CFLAGS_ksu.o += $(call cc-option, -std=gnu2y)
 
+# Linux 3.4 headers use GNU89 extern inline semantics (e.g. ARM ftrace).
+ifeq ($(VERSION).$(PATCHLEVEL),3.4)
+CFLAGS_ksu.o += $(call cc-option, -fgnu89-inline)
+endif
+
 #ifeq ($(shell $(CC) -v 2>&1 | grep -q 'clang'; echo $$?),0)
 # $(info -- KernelSU/CC: clang!)
 #endif
diff --git a/kernel/external/linux_overflow.h b/kernel/external/linux_overflow.h
index fd465c8..a397822 100644
--- a/kernel/external/linux_overflow.h
+++ b/kernel/external/linux_overflow.h
@@ -6,6 +6,12 @@
 #ifndef __LINUX_OVERFLOW_H
 #define __LINUX_OVERFLOW_H
 
+#include <linux/types.h>
+
+#ifndef SIZE_MAX
+#define SIZE_MAX ((size_t)~0UL)
+#endif
+
 /*
  * In the fallback code below, we need to compute the minimum and
  * maximum values representable in a given type. These macros may also
diff --git a/kernel/policy/app_profile.c b/kernel/policy/app_profile.c
index 4e12aab..11cbbcf 100644
--- a/kernel/policy/app_profile.c
+++ b/kernel/policy/app_profile.c
@@ -1,3 +1,5 @@
+#include <linux/prctl.h>
+
 #if LINUX_VERSION_CODE >= KERNEL_VERSION (6, 7, 0)
 static struct group_info root_groups = { .usage = REFCOUNT_INIT(2) };
 #else
@@ -143,7 +145,9 @@ static int escape_to_root(bool is_forced)
 	memcpy(&cred->cap_bset, &profile->capabilities.effective, sizeof(cred->cap_bset));
 	if (profile->uid != 0) {
 		memcpy(&cred->cap_inheritable, &profile->capabilities.effective, sizeof(cred->cap_inheritable));
+#ifdef PR_CAP_AMBIENT
 		memcpy(&cred->cap_ambient, &profile->capabilities.effective, sizeof(cred->cap_ambient));
+#endif
 	}
 
 	setup_groups(profile, cred);
diff --git a/kernel/selinux/rules.c b/kernel/selinux/rules.c
index b510ba2..c611fb1 100644
--- a/kernel/selinux/rules.c
+++ b/kernel/selinux/rules.c
@@ -83,12 +83,14 @@ static int apply_kernelsu_rules_fn(void *ptr)
 	ksu_allow(db, KERNEL_SU_DOMAIN, ALL, ALL, ALL);
 
 	// allow us do any ioctl
+#ifndef KSU_COMPAT_NO_XPERMS
 	if (db->policyvers >= POLICYDB_VERSION_XPERMS_IOCTL) {
 		ksu_allowxperm(db, KERNEL_SU_DOMAIN, ALL, "blk_file", ALL);
 		ksu_allowxperm(db, KERNEL_SU_DOMAIN, ALL, "fifo_file", ALL);
 		ksu_allowxperm(db, KERNEL_SU_DOMAIN, ALL, "chr_file", ALL);
 		ksu_allowxperm(db, KERNEL_SU_DOMAIN, ALL, "file", ALL);
 	}
+#endif
 
 	// our ksud triggered by init
 	ksu_allow(db, "init", KERNEL_SU_DOMAIN, ALL, ALL);
diff --git a/kernel/selinux/sepolicy.c b/kernel/selinux/sepolicy.c
index 11a8897..d945ce1 100644
--- a/kernel/selinux/sepolicy.c
+++ b/kernel/selinux/sepolicy.c
@@ -1,5 +1,9 @@
 #define KSU_SUPPORT_ADD_TYPE
 
+#if defined(AVTAB_OP) && !defined(AVTAB_XPERMS)
+#define KSU_COMPAT_NO_XPERMS
+#endif
+
 /*
  * Adapt to Huawei HISI kernel without affecting other kernels ,
  * Huawei Hisi Kernel EBITMAP Enable or Disable Flag ,
@@ -18,8 +22,14 @@
 // Declaration
 //////////////////////////////////////////////////////
 
+#ifdef KSU_COMPAT_NO_XPERMS
+typedef void ksu_avtab_extended_perms;
+#else
+typedef struct avtab_extended_perms ksu_avtab_extended_perms;
+#endif
+
 static struct avtab_node *get_avtab_node(struct policydb *db, struct avtab_key *key,
-                                         struct avtab_extended_perms *xperms);
+					 ksu_avtab_extended_perms *xperms);
 
 static bool is_redundant_avtab_node(struct avtab_node *node);
 
@@ -31,10 +41,12 @@ static bool add_rule(struct policydb *db, const char *s, const char *t, const ch
 static bool add_rule_raw(struct policydb *db, struct type_datum *src, struct type_datum *tgt, struct class_datum *cls,
 				struct perm_datum *perm, int effect, bool invert);
 
+#ifndef KSU_COMPAT_NO_XPERMS
 static void add_xperm_rule_raw(struct policydb *db, struct type_datum *src, struct type_datum *tgt,
 				struct class_datum *cls, uint16_t low, uint16_t high, int effect, bool invert);
 static bool add_xperm_rule(struct policydb *db, const char *s, const char *t, const char *c, const char *range,
 				int effect, bool invert);
+#endif
 
 static bool add_type_rule(struct policydb *db, const char *s, const char *t, const char *c, const char *d, int effect);
 
@@ -83,10 +95,11 @@ static bool add_typeattribute(struct policydb *db, const char *type, const char
 
 static struct avtab_node *get_avtab_node(struct policydb *db,
 					 struct avtab_key *key,
-					 struct avtab_extended_perms *xperms)
+					 ksu_avtab_extended_perms *xperms)
 {
 	struct avtab_node *node;
 
+#ifndef KSU_COMPAT_NO_XPERMS
 	/* AVTAB_XPERMS entries are not necessarily unique */
 	if (key->specified & AVTAB_XPERMS) {
 		bool match = false;
@@ -105,6 +118,9 @@ static struct avtab_node *get_avtab_node(struct policydb *db,
 	} else {
 		node = avtab_search_node(&db->te_avtab, key);
 	}
+#else
+	node = avtab_search_node(&db->te_avtab, key);
+#endif
 
 	if (!node) {
 		struct avtab_datum avdatum = {};
@@ -112,12 +128,16 @@ static struct avtab_node *get_avtab_node(struct policydb *db,
      * AUDITDENY, aka DONTAUDIT, are &= assigned, versus |= for
      * others. Initialize the data accordingly.
      */
+#ifndef KSU_COMPAT_NO_XPERMS
 		if (key->specified & AVTAB_XPERMS) {
 			avdatum.u.xperms = xperms;
 		} else {
+#endif
 			avdatum.u.data =
 				key->specified == AVTAB_AUDITDENY ? ~0U : 0U;
+#ifndef KSU_COMPAT_NO_XPERMS
 		}
+#endif
 		/* this is used to get the node - insertion is actually unique */
 		node = avtab_insert_nonunique(&db->te_avtab, key, &avdatum);
 		if (!node)
@@ -125,12 +145,14 @@ static struct avtab_node *get_avtab_node(struct policydb *db,
 
 		int grow_size = sizeof(struct avtab_key);
 		grow_size += sizeof(struct avtab_datum);
+#ifndef KSU_COMPAT_NO_XPERMS
 		if (key->specified & AVTAB_XPERMS) {
 			grow_size += sizeof(u8);
 			grow_size += sizeof(u8);
 			grow_size += sizeof(u32) *
 				     ARRAY_SIZE(avdatum.u.xperms->perms.p);
 		}
+#endif
 		db->len += grow_size;
 	}
 
@@ -139,8 +161,10 @@ static struct avtab_node *get_avtab_node(struct policydb *db,
 
 static bool is_redundant_avtab_node(struct avtab_node *node)
 {
+#ifndef KSU_COMPAT_NO_XPERMS
 	if (node->key.specified & AVTAB_XPERMS)
 		return node->datum.u.xperms == NULL;
+#endif
 	if (!(node->key.specified & AVTAB_AV))
 		return false;
 	if (node->key.specified & AVTAB_AUDITDENY)
@@ -205,9 +229,11 @@ static bool remove_avtab_node(struct policydb *db, struct avtab_node *node)
 			if (db->te_avtab.nel > 0)
 				db->te_avtab.nel--;
 
+#ifndef KSU_COMPAT_NO_XPERMS
 			if ((n->key.specified & AVTAB_XPERMS) && n->datum.u.xperms) {
 				shrink_size += sizeof(u8) + sizeof(u8) + sizeof(u32) * ARRAY_SIZE(n->datum.u.xperms->perms.p);
 			}
+#endif
 			n->next = NULL;
 			avtab_set_slot(&removed, 0, n);
 			removed.nel = 1;
@@ -352,6 +378,8 @@ static bool add_rule_raw(struct policydb *db, struct type_datum *src, struct typ
 	return success;
 }
 
+#ifndef KSU_COMPAT_NO_XPERMS
+
 #define ioctl_driver(x) (x >> 8 & 0xFF)
 #define ioctl_func(x) (x & 0xFF)
 
@@ -488,6 +516,8 @@ static bool add_xperm_rule(struct policydb *db, const char *s, const char *t, co
 	return true;
 }
 
+#endif /* !KSU_COMPAT_NO_XPERMS */
+
 static bool add_type_rule(struct policydb *db, const char *s, const char *t, const char *c, const char *d, int effect)
 {
 	struct type_datum *src, *tgt, *def;
@@ -1171,22 +1201,34 @@ bool ksu_dontaudit(struct policydb *db, const char *src, const char *tgt,
 bool ksu_allowxperm(struct policydb *db, const char *src, const char *tgt,
 		    const char *cls, const char *range)
 {
+#ifdef KSU_COMPAT_NO_XPERMS
+	return false;
+#else
 	return add_xperm_rule(db, src, tgt, cls, range, AVTAB_XPERMS_ALLOWED,
 			      false);
+#endif
 }
 
 bool ksu_auditallowxperm(struct policydb *db, const char *src, const char *tgt,
 			 const char *cls, const char *range)
 {
+#ifdef KSU_COMPAT_NO_XPERMS
+	return false;
+#else
 	return add_xperm_rule(db, src, tgt, cls, range, AVTAB_XPERMS_AUDITALLOW,
 			      false);
+#endif
 }
 
 bool ksu_dontauditxperm(struct policydb *db, const char *src, const char *tgt,
 			const char *cls, const char *range)
 {
+#ifdef KSU_COMPAT_NO_XPERMS
+	return false;
+#else
 	return add_xperm_rule(db, src, tgt, cls, range, AVTAB_XPERMS_DONTAUDIT,
 			      false);
+#endif
 }
 
 // Type rules
